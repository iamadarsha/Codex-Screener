# Cloud Scheduler Jobs

The production scheduler will manage three IST-timed jobs:

- `08:00` instrument sync and universe refresh
- `08:45` Upstox token refresh
- `09:00` market stream bootstrap

The exact deployment commands will be added in the deployment phase once the
runtime services exist.

